# AppAssets.xcassets

Place app icon image files into `AppIcon.appiconset` and update `Contents.json` with the corresponding `filename` values.

This asset catalog is created for Xcode-native icon management.
